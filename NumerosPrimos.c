#include <stdio.h>

int main (){
    int num, cont, divisor, simPri,naoPri;
    simPri=1; // quando for primo
    naoPri=0; // quando nao for primo
    divisor=2;
    printf ("Escreva um numero");
    scanf ("%d", &num);
    while (num<=1){
        printf("Numero invalido");
        scanf ("%d", &num);

    }





return 0;
}
